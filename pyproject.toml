[project]
name = "roguedungeonstests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "opensimplex>=0.4.5.1",
    "pyglet>=2.1.14",
    "transitions>=0.9.3",
]
